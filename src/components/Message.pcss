.message {
  color: #999;
  font-size: 2rem;
  font-weight: 300;
  line-height: 1.35;
  text-align: center;

  > span,
  > svg {
    vertical-align: middle;
  }
}

.message :global(.icon) {
  display: inline-block;
  height: .85em;
  margin-right: .4em;
  width: .85em;
}
