@import "../../styles/colors";


.header {
  background: linear-gradient(0deg, color(#783687 a(0.5)), color(#4F227F a(0.5)));
  box-shadow: 0 -2px 4px 0px rgba(0, 0, 0, 0.075) inset;
  display: flex;
  overflow: hidden;
  padding: grid(2) grid(1);
  position: relative;
  z-index: 3;
}
